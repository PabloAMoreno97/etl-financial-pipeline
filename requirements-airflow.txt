# Only packages not already bundled by the Airflow image.
# DO NOT add sqlalchemy, psycopg2, or requests here — Airflow ships its own
# pinned versions and upgrading them breaks Airflow's internal ORM models.
pandas==2.2.3
numpy==2.1.3
pydantic-settings==2.6.1
